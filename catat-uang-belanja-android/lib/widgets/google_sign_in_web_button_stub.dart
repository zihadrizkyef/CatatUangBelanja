import 'package:flutter/material.dart';

/// Non-web stub — [LoginView] only ever calls this behind a `kIsWeb` check,
/// so this body never actually runs; it exists purely so the app compiles
/// on Android/iOS without pulling in google_sign_in_web's web-only APIs.
Widget renderGoogleSignInWebButton() {
  throw UnsupportedError('renderGoogleSignInWebButton is web-only');
}
