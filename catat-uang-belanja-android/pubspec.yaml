name: catat_uang_belanja
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State management: single app-wide ChangeNotifier (FinanceRepository)
  provider: ^6.1.2

  # Local storage: SQLite (native mobile) + FFI backends for desktop/web
  sqflite: ^2.4.1
  sqflite_common_ffi: ^2.3.4+4
  sqflite_common_ffi_web: ^1.1.2
  path: ^1.9.1

  # Model IDs (Wallet/Category/Transaction/Budget all use string/UUID ids)
  uuid: ^4.5.1
  # Rupiah currency formatting + date formatting
  intl: ^0.20.2
  # Renders bundled OpenMoji SVGs so category/wallet/status icons look identical
  # across every device and OS, instead of relying on the platform's emoji font.
  flutter_svg: ^2.0.10+1

  # App-lock (doc 4.11): Keystore/Keychain-backed storage for the PIN hash+salt
  # (and, later, auth tokens) — never kept in the plaintext sqlite settings table.
  flutter_secure_storage: ^9.2.4
  # Biometric (fingerprint/Face ID) prompt for app-lock.
  local_auth: ^2.3.0
  # PBKDF2-HMAC-SHA256 PIN hashing (pure Dart, no native build step).
  crypto: ^3.0.6

  # Online sync (doc 4.10): Google Sign-In as the login method, HTTP client
  # for the backend API, and connectivity change detection to trigger
  # background sync automatically once the device is back online.
  google_sign_in: ^7.1.1
  # Direct dependency (not just transitive via google_sign_in) because
  # LoginView imports package:google_sign_in_web/web_only.dart directly to
  # render Google's own button widget — required on web, which doesn't
  # allow a custom button to trigger sign-in (doc 4.10).
  google_sign_in_web: ^1.1.3
  http: ^1.2.2
  connectivity_plus: ^6.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Generates Android/iOS launcher icons from assets/icon/*.png (dev-time only,
  # run via `dart run flutter_launcher_icons`) — see flutter_launcher_icons: below.
  flutter_launcher_icons: ^0.14.4

# Wallet + coin app icon (see Draft Aplikasi Pencatatan Keuangan.md section 2 for
# the palette). Regenerate with `dart run flutter_launcher_icons` after editing
# the source PNGs in assets/icon/.
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon.png"
  adaptive_icon_background: "#E8637C"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"
  remove_alpha_ios: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # OpenMoji SVGs (see assets/icons/openmoji/NOTICE.md for license/attribution)
  # used for category/wallet/status icons so they render identically on every
  # device and OS instead of depending on the platform's emoji font.
  #
  # assets/icons/custom/ holds a handful of hand-drawn icons (fingerprint,
  # extra avatar choices) in the same flat/line-art style, for cases with no
  # matching bundled OpenMoji asset — these are original art, not OpenMoji, so
  # they're kept out of assets/icons/openmoji/NOTICE.md's CC BY-SA attribution.
  assets:
    - assets/icons/openmoji/
    - assets/icons/custom/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # Bundled locally (not google_fonts) so the UI renders correctly offline on
  # first launch. Baloo2 = headings, Nunito = body text.
  fonts:
    - family: Baloo2
      fonts:
        - asset: assets/fonts/Baloo2-Variable.ttf
        - asset: assets/fonts/Baloo2-Variable.ttf
          weight: 700
    - family: Nunito
      fonts:
        - asset: assets/fonts/Nunito-Variable.ttf
        - asset: assets/fonts/Nunito-Variable.ttf
          weight: 700

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
